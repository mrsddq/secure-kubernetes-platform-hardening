apiVersion: v1
kind: Service
metadata:
  name: secure-api
  namespace: secure-app
spec:
  selector:
    app.kubernetes.io/name: secure-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
