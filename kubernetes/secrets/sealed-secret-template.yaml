apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: secure-api-config
  namespace: secure-app
spec:
  encryptedData:
    DATABASE_URL: replace-with-sealed-secret-ciphertext
  template:
    metadata:
      name: secure-api-config
      namespace: secure-app
